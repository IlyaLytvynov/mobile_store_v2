@import"../variables.styl";

main
	min-height 100%
	background $contentColor
	position relative
	display flex
	flex-direction column
	justify-content
	>ui-view
		height 80%
		flex-grow 2
		width 100%
		max-width $defaultContainerWidth
		box-sizing border-box
		margin 0 auto
	footer
		position relative
		bottom 0
		flex-grow 1
